# Lexical Analyzer Lab

## Project Description
A lightweight lexical analyzer implemented in Python for tokenizing source code. (一个轻量级的 Python 词法分析器，用于源代码的分词。)

This tool reads an input source file, identifies tokens based on predefined lexical rules (such as keywords, identifiers, operators, and delimiters), and outputs the recognized token sequence.

## GitHub Repository Details

### Repository Name
`Lexical-Analyzer-Lab`

### Description
A lightweight lexical analyzer implemented in Python for tokenizing source code.

### Topics (Tags)
`compiler-construction`, `lexical-analysis`, `lexer`, `python`, `tokenization`

## How to Run

1. Ensure you have Python installed.
2. Place your test code in `input.txt`.
3. Run the script:
   ```bash
   python lexer.py
   ```
